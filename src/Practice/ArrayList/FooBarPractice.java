package Practice.ArrayList;

import java.util.ArrayList;
import java.util.Arrays;

public class FooBarPractice {

    public static void main(String[] args) {


        /* REGULAR ARRAY
         int[] numbers = {100, 35, 44, 88, 11, 233, 0, 500, 24};


        int max = numbers[0];
        int min = numbers[0];

        for (int i = 0; i < numbers.size(); i++) {
            if (numbers[i] < min) min = numbers[i];
            if (numbers[i] > max) max = numbers[i];
        }


        System.out.println(max);
        System.out.println(min); */
    }}

    /* ArrayList<Integer> numbers = new ArrayList<>(Arrays.asList(100, 35, 44, 88, 11, 233, 0, 500, 24));

    int min = Integer.MAX_VALUE;
    int max = Integer.MIN_VALUE;


        for (int i = 0; i < numbers.size(); i++) {
        if (numbers.get(i) < min) min = numbers.get(i);
        if (numbers.get(i) > max) max = numbers.get(i);


    }
    int max2 = Integer.MIN_VALUE;
    int min2 = Integer.MAX_VALUE;

        for (int i = 0; i < numbers.size(); i++) {
        if(min != numbers.get(i) && numbers.get(i) < min2) min2 = numbers.get(i);
        if(max != numbers.get(i) && numbers.get(i) > max2) max2 = numbers.get(i);

    }

        System.out.println(max2);
        System.out.println(min2);

}


}
}
     */
